# 终极部署指南 (hackins.club 专用版)

您已经拥有了完美的域名 `hackins.club`。现在的目标是将它连接到 Cloudflare，打通国内访问。

## 第一阶段：域名“过户” (Aliyun -> Cloudflare)
您的域名目前在阿里云 (HiChina)，我们需要把它的 DNS 管理权交给 Cloudflare。

1.  **在 Cloudflare 获取 Nameservers**:
    *   在 Cloudflare 看到 `Invalid nameservers` 的那个页面，往下翻。
    *   它会提示您两条记录，通常长这样：
        *   `xxxx.ns.cloudflare.com`
        *   `yyyy.ns.cloudflare.com`
    *   **复制这两条地址**。

2.  **在阿里云修改**:
    *   登录 [阿里云域名控制台](https://dc.console.aliyun.com/)。
    *   找到 `hackins.club` -> 点击 **管理**。
    *   左侧菜单点击 **DNS修改** -> **修改DNS服务器**。
    *   将原本的 `dns25.hichina.com` 等删掉，**填入 Cloudflare 给您的那两条**。
    *   保存。

3.  **等待生效**:
    *   这可能需要 10 分钟到几小时。
    *   当 Cloudflare 控制台显示 ✅ **Active** (绿色) 时，说明成功了。

---

## 第二阶段：绑定网站 (Pages)
让用户访问 `hackins.club` 时显示您的网页。

1.  在 Cloudflare 后台 -> **Workers & Pages** -> 点击您的前端项目 (strangers)。
2.  点击 **Custom Domains** -> **Set up a Custom Domain**。
3.  输入 `hackins.club` (如果不让输根域名，也可以输 `www.hackins.club`，但这需要之后的额外跳转配置，尽量尝试根域名)。
4.  点击 **Activate Domain**。Cloudflare 会自动为您添加 DNS 记录。

---

## 第三阶段：绑定后端 (Worker)
让 `hackins.club/api` 处理数据请求 (这样国内也能访问后端)。

1.  在 Cloudflare 后台 -> **Workers & Pages** -> 点击您的后端项目 (strangers-backend)。
2.  点击 **Settings** -> **Triggers**。
3.  找到 **Routes** 部分 -> 点击 **Add Route**。
4.  **Route**: 输入 `hackins.club/api/*`
    *   ⚠️ 注意：如果是 `www.hackins.club`，则填 `www.hackins.club/api/*`。
5.  **Zone**: 选择 `hackins.club`。
6.  点击 **Add Route**。

---

## 第四阶段：发布代码
既然配置文件 `assets/config.js` 我已经帮您改好了 (指向 `https://hackins.club`)，您只需要最后提交一次代码：

```bash
git add .
git commit -m "Config: Update for hackins.club"
git push origin main
```

**大功告成！**
等几分钟 DNS 全球生效后，打开 `https://hackins.club`，您的专属树洞就正式上线了，国内国外速度起飞 🚀。
